# Stage 4 output

Your **backlog snapshot** lands here as `backlog-snapshot.md` — the full list of tasks, saved as a file so later stages can read it without needing GitHub.

Produced by following [`../README.md`](../README.md). This folder is empty until you do.
